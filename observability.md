# Observability

StarDot emphasizes Trust — everything is traceable.

```json
"observability": {
  "metrics": ["call_count","error_rate","denied"],
  "trace": true,
  "logLevel": "info"
}
```

## Metrics endpoints
- `/metrics/validation`
- `/metrics/enforcement`
